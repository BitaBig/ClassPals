import { Router } from 'express';

const router = Router();

// Post routes - to be implemented
// GET /api/courses/:courseId/posts - Get all posts for a course
// POST /api/courses/:courseId/posts - Create a new post

export default router;
